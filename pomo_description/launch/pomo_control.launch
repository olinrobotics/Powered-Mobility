<?xml version="1.0"?>
<launch>
  <!-- Load joint controller configurations from YAML file to parameter server -->
  <rosparam command="load" file="$(find pomo_description)/config/pomo_control.yaml"/>
  <!-- load the controllers -->
  <node name="controller_spawner" pkg="controller_manager" type="spawner"
	  output="screen" ns="/pomo" args="joint_state_controller velocity_controller lidar_controller"/>
</launch>
